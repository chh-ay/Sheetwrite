import "@sheetwrite/core/styles.css";

const marker = document.createElement("div");
marker.className = "sheetwrite-packed-consumer";
marker.textContent = "Sheetwrite packed consumer";
document.body.append(marker);
