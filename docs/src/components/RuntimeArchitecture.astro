<section class="sw-architecture" aria-label="Sheetwrite runtime ownership and data flow">
  <header class="sw-architecture-host">
    <div class="sw-architecture-heading">
      <span>Browser composition</span>
      <strong>Grid host</strong>
    </div>
    <code>role="grid" · .sheetwrite · tabindex="0"</code>
  </header>

  <div class="sw-architecture-surfaces">
    <article>
      <span class="sw-architecture-eyebrow">Interface</span>
      <strong>Controls</strong>
      <p>Optional toolbar and workbook tabs.</p>
    </article>

    <article class="is-viewport">
      <span class="sw-architecture-eyebrow">Render path</span>
      <strong>Viewport</strong>
      <p>Native scroller → canvas or Worker renderer → selection overlay.</p>
    </article>

    <article>
      <span class="sw-architecture-eyebrow">Accessibility</span>
      <strong>ARIA mirror</strong>
      <p>Screen-reader tree kept outside the canvas.</p>
    </article>
  </div>

  <div class="sw-architecture-route" aria-hidden="true">
    <span>one bulk window per painted pane</span>
    <i>↓</i>
  </div>

  <div class="sw-architecture-engines">
    <article>
      <span class="sw-architecture-eyebrow">TypeScript core</span>
      <strong>SheetwriteStore</strong>
      <p>Sequences document transactions and exposes resolved viewport windows.</p>
    </article>

    <div class="sw-architecture-ffi" aria-label="Batched foreign function interface">
      <span>batched FFI</span>
      <i aria-hidden="true">→</i>
    </div>

    <article class="is-engine">
      <span class="sw-architecture-eyebrow">Rust / WebAssembly</span>
      <strong>Columnar engine</strong>
      <p>Owns typed cell storage, formula evaluation, and the calculation DAG.</p>
    </article>
  </div>
</section>

<style>
  .sw-architecture,
  .sw-architecture * {
    box-sizing: border-box;
  }

  .sw-architecture {
    margin-block: var(--sw-space-6) var(--sw-space-7);
    overflow: hidden;
    border: var(--sw-line) solid var(--sw-border-strong);
    border-radius: var(--sw-radius-md);
    background: var(--sw-surface-canvas);
  }

  .sw-architecture-host {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: center;
    gap: var(--sw-space-5);
    border-bottom: var(--sw-line) solid var(--sw-border-strong);
    padding: var(--sw-space-5) var(--sw-space-6);
    background: var(--sw-surface-raised);
  }

  .sw-architecture-heading,
  .sw-architecture-surfaces article,
  .sw-architecture-engines article {
    display: grid;
    gap: var(--sw-space-2);
  }

  .sw-architecture-heading span,
  .sw-architecture-eyebrow {
    color: var(--sw-text-subtle);
    font: 600 0.625rem/1.2 var(--sw-font-code);
    letter-spacing: 0.09em;
    text-transform: uppercase;
  }

  .sw-architecture :is(.sw-architecture-heading, article) strong {
    color: var(--sw-text);
    font: 650 var(--sw-text-lg) / 1.2 var(--sw-font-display);
  }

  .sw-architecture-host code {
    color: var(--sw-text-muted);
    font-size: var(--sw-text-xs);
    white-space: nowrap;
  }

  .sw-architecture-surfaces {
    display: grid;
    grid-template-columns: minmax(0, 0.8fr) minmax(16rem, 1.4fr) minmax(0, 0.8fr);
  }

  .sw-architecture-surfaces article {
    min-width: 0;
    padding: var(--sw-space-5);
  }

  .sw-architecture-surfaces article + article {
    border-inline-start: var(--sw-line) solid var(--sw-border);
  }

  .sw-architecture-surfaces .is-viewport {
    background: color-mix(in srgb, var(--sw-code-type) 6%, var(--sw-surface-canvas));
  }

  .sw-architecture article p {
    margin: 0;
    color: var(--sw-text-muted);
    font-size: var(--sw-text-sm);
    line-height: 1.55;
  }

  .sw-architecture-route {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--sw-space-3);
    border-block: var(--sw-line) solid var(--sw-border);
    padding: var(--sw-space-3) var(--sw-space-5);
    background: var(--sw-surface-code);
    color: var(--sw-text-subtle);
    font: 600 var(--sw-text-xs) / 1.2 var(--sw-font-body);
    letter-spacing: 0.02em;
  }

  .sw-architecture-route i,
  .sw-architecture-ffi i {
    color: var(--sw-brand-strong);
    font-style: normal;
  }

  .sw-architecture-engines {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
    align-items: stretch;
  }

  .sw-architecture-engines article {
    min-width: 0;
    padding: var(--sw-space-6);
  }

  .sw-architecture-engines .is-engine {
    background: color-mix(in srgb, var(--sw-brand) 7%, var(--sw-surface-canvas));
  }

  .sw-architecture-ffi {
    display: grid;
    min-width: 6.5rem;
    place-content: center;
    justify-items: center;
    gap: var(--sw-space-2);
    border-inline: var(--sw-line) solid var(--sw-border);
    color: var(--sw-text-subtle);
    font: 600 var(--sw-text-xs) / 1.2 var(--sw-font-body);
    letter-spacing: 0.02em;
  }

  .sw-architecture-ffi i {
    font-size: 1.25rem;
  }

  @media (max-width: 52rem) {
    .sw-architecture-host {
      grid-template-columns: 1fr;
      gap: var(--sw-space-3);
      padding: var(--sw-space-5);
    }

    .sw-architecture-host code {
      white-space: normal;
    }

    .sw-architecture-surfaces {
      grid-template-columns: 1fr;
    }

    .sw-architecture-surfaces article + article {
      border-block-start: var(--sw-line) solid var(--sw-border);
      border-inline-start: 0;
    }

    .sw-architecture-engines {
      grid-template-columns: 1fr;
    }

    .sw-architecture-engines article {
      padding: var(--sw-space-5);
    }

    .sw-architecture-ffi {
      min-height: 4rem;
      border-block: var(--sw-line) solid var(--sw-border);
      border-inline: 0;
    }

    .sw-architecture-ffi i {
      transform: rotate(90deg);
    }
  }
</style>
